# railway.toml
[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run db:generate && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "boulder-backend"
